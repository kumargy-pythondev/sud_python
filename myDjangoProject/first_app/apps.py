from django.apps import AppConfig


class FirstAppConfig(AppConfig):
    name = 'first_app'
#the above line is just a variable for a new app created 'first_app'
# you need not worry about this so much. it is fine
# again I am adding this for additional practice